<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <!-- Collapsed Pill (Dynamic Island) -->
    <LinearLayout
        android:id="@+id/pillView"
        android:layout_width="140dp"
        android:layout_height="44dp"
        android:background="@drawable/bg_dynamic_island_pill"
        android:gravity="center"
        android:orientation="horizontal"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:elevation="12dp"
        android:visibility="visible">

        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_launcher_foreground"
            android:scaleType="centerCrop"
            android:layout_marginEnd="8dp"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="OmniAgent"
            android:textColor="#FFFFFFFF"
            android:textSize="12sp"
            android:textStyle="bold"/>
    </LinearLayout>

    <!-- Expanded Chat View -->
    <LinearLayout
        android:id="@+id/expandedView"
        android:layout_width="320dp"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_floating_chat"
        android:orientation="vertical"
        android:padding="16dp"
        android:elevation="16dp"
        android:visibility="gone">

        <!-- Header Bar -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="12dp">

            <View
                android:layout_width="8dp"
                android:layout_height="8dp"
                android:background="@drawable/ic_orb_pulse"
                android:layout_marginEnd="8dp"/>

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="OmniAgent AI"
                android:textColor="#FFFFFFFF"
                android:textStyle="bold"
                android:textSize="13sp"/>

            <ImageButton
                android:id="@+id/btnClose"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:src="@android:drawable/ic_menu_close_clear_cancel"
                android:background="@android:color/transparent"
                android:tint="#88FFFFFF"/>
        </LinearLayout>

        <!-- AI Response area -->
        <TextView
            android:id="@+id/tvAiResponse"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="56dp"
            android:maxHeight="180dp"
            android:scrollbars="vertical"
            android:overScrollMode="always"
            android:text="Ask me anything..."
            android:textColor="#CCFFFFFF"
            android:textSize="12sp"
            android:lineSpacingMultiplier="1.4"
            android:padding="8dp"
            android:background="#1AFFFFFF"
            android:backgroundTintMode="src_over"
            android:layout_marginBottom="12dp"/>

        <!-- Input Row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="44dp"
            android:orientation="horizontal"
            android:background="@drawable/bg_input_row"
            android:gravity="center_vertical"
            android:paddingStart="12dp"
            android:paddingEnd="4dp">

            <EditText
                android:id="@+id/etAiInput"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:hint="Ask anything..."
                android:textColor="#FFFFFFFF"
                android:textSize="13sp"
                android:background="@android:color/transparent"
                android:singleLine="true"
                android:imeOptions="actionSend"/>

            <ImageButton
                android:id="@+id/btnSend"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:src="@android:drawable/ic_menu_send"
                android:background="@drawable/bg_send_btn"
                android:tint="#FFFFFFFF"
                android:padding="8dp"/>
        </LinearLayout>

    </LinearLayout>

</FrameLayout>
